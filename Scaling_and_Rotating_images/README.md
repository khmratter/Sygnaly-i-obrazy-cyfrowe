# Image scaling and rotating

Image scaling is the resizing (either upsizing or downsizing) of an image. 
Image rotating is the rotation of the original image by some angle.  

Both of these actions represent the application of interpolation. When scaling the image, a new image with a higher or lower number of pixels must be generated. The goal is to minimize the quality loss after the conversion. 
